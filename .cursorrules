## コマンド実行権限
- 以下の目的のためのターミナルコマンド実行を許可します：
  - 依存関係のインストール（例：`npm install`, `pip install`）
  - ビルドおよびテストの実行（例：`npm run build`, `pytest`）
  - プロジェクト構造の調査（例：`ls -R`, `grep`, `find`）
- コマンドを実行する前に、必ずその目的を日本語で説明してください。
- 破壊的なコマンド（例：`rm`, データベースの削除など）を実行する場合は、必ず事前に明示的な確認を求めてください。